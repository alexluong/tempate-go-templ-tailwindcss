services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /app
    ports:
      - "8090:8090"
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - .git
